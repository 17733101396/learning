<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hainu.hrms.mapper.EmployeeMapper">

	<resultMap type="Employee" id="EmployeeResult">
		<id property="id" column="emp_id" />
		<result property="name" column="emp_name" />
		<result property="sex" column="sex" />
		<result property="phone" column="phone" />
		<result property="email" column="email" />
		<result property="address" column="address" />
		<result property="birthday" column="birthday" />
		<result property="dept_id" column="dept_id" />
		<result property="pos_id" column="pos_id" />
	</resultMap>

	<select id="selectEmployees" resultMap="EmployeeResult">
		select * from employee_tb
	</select>
	
	<delete id="deleteEmployees" parameterType="java.util.List">
		delete from employee_tb
		where
		emp_id in (<foreach collection="list"  item="id" separator=",">#{id}</foreach>)
	</delete>
	
	<insert id="addEmployee" parameterType="Employee" useGeneratedKeys="true" keyProperty="emp_id">
		insert
		into employee_tb(emp_name,sex,phone,email,address,birthday,dept_id,pos_id)
		values(#{name},#{sex},#{phone},#{email},#{address},#{birthday},#{dept_id},#{pos_id})
	</insert>

	<update id="updateEmployee" parameterType="Employee">
		update employee_tb set
		emp_name=#{name},sex=#{sex},phone=#{phone},email=#{email},address=#{address},birthday=#{birthday},dept_id=#{dept_id},pos_id=#{pos_id}
		where emp_id=#{id}
	</update>
	
</mapper>