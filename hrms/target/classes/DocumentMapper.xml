<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hainu.hrms.mapper.DocumentMapper">

	<resultMap type="Document" id="DocumentResult">
		<id property="id" column="doc_id" />
		<result property="name" column="doc_name" />
		<result property="create_date" column="create_date" />
		<result property="admin_id" column="admin_id" />
	</resultMap>
	
	<select id="selectDocumentsByUser" parameterType="Document" resultMap="DocumentResult">
		select * from document_tb where admin_id=#{admin_id}
	</select>
	
	<select id="selectDocuments" resultMap="DocumentResult">
		select * from document_tb
	</select>
	
	<delete id="deleteDocuments" parameterType="java.util.List">
		delete from document_tb
		where
		doc_id in (<foreach collection="list"  item="id" separator=",">#{id}</foreach>)
	</delete>
	
	<insert id="addDocument" parameterType="Document" useGeneratedKeys="true" keyProperty="doc_id">
		insert
		into document_tb(doc_name,admin_id,create_date)
		values(#{name},#{admin_id},#{create_date})
	</insert>

	<update id="updateDocument" parameterType="Document">
		update document_tb set
		doc_name=#{name},admin_id=#{admin_id},create_date=#{create_date}
		where doc_id=#{id}
	</update>
	
</mapper>